apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: test-http
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`kotatsu.kanp.ai`)
      kind: Rule
      services:
        - name: test
          scheme: http
          port: 80
