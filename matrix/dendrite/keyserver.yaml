apiVersion: v1
kind: Service
metadata:
  name: keyserver
spec:
  ports:
    - port: 7779
      name: keyserver-api-internal
  selector:
    app: keyserver
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: keyserver
spec:
  replicas: 1
  revisionHistoryLimit: 1
  selector:
    matchLabels:
      app: keyserver
  template:
    metadata:
      labels:
        app: keyserver
    spec:
      volumes:
        - name: config
          configMap:
            name: dendrite-config
        - name: certificate
          secret:
            secretName: matrix-tls
            optional: false
        - name: matrix-key
          secret:
            secretName: matrix-key
            optional: false
        - name: matrix-key-old
          projected:
            sources:
              - secret:
                  name: old-matrix-keys
      containers:
        - name: keyserver
          image: docker.io/matrixdotorg/dendrite-polylith:latest
          args: ["keyserver"]
          volumeMounts:
            - name: config
              subPath: config.yaml
              mountPath: /etc/dendrite/dendrite.yaml
              readOnly: true
            - name: certificate
              subPath: tls.crt
              mountPath: /etc/dendrite/server.crt
              readOnly: true
            - name: certificate
              subPath: tls.key
              mountPath: /etc/dendrite/server.key
              readOnly: true
            - name: matrix-key
              subPath: private.key
              mountPath: /etc/dendrite/matrix_key.pem
              readOnly: true
            - name: matrix-key-old
              mountPath: /etc/dendrite/old-keys/
              readOnly: true
          # tcp liveness probe on api port
---
apiVersion: v1
kind: Service
metadata:
  name: keyserver-database
  labels:
    app: keyserver-database
spec:
  ports:
    - port: 5432
      name: database
  selector:
    app: keyserver-database
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: keyserver-database
spec:
  selector:
    matchLabels:
      app: keyserver-database
  serviceName: "keyserver-database"
  replicas: 1
  template:
    metadata:
      labels:
        app: keyserver-database
    spec:
      volumes:
        - name: database
          persistentVolumeClaim:
            claimName: keyserver-database
      containers:
        - name: postgres
          image: docker.io/postgres:14.4-alpine
          ports:
            - containerPort: 5432
              name: postgres
          volumeMounts:
            - name: database
              mountPath: /var/lib/postgresql
          env:
            - name: POSTGRES_DB
              value: "keyserver"
            - name: POSTGRES_USER
              value: "keyserver"
            - name: POSTGRES_PASSWORD
              value: "keyserver" # use sealed secret
          livenessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - keyserver
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: keyserver-database
spec:
  storageClassName: scw-bssd
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
