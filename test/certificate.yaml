apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: test-cert
spec:
  issuerRef:
    kind: ClusterIssuer
    name: scaleway-letsencrypt
  secretName: test-tls
  dnsNames:
    - test.mib.dev
