apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: test-cert
  namespace: istio-ingress
spec:
  issuerRef:
    kind: ClusterIssuer
    name: scaleway-letsencrypt
  secretName: test-tls
  dnsNames:
    - test.kanp.ai
